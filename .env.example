DATABASE_URL=            # Neon connection string (required for everything)
MAPTILER_KEY=            # optional; falls back to demo style
GBIF_USER=               # only needed for Phase 3 batch download
GBIF_PASS=
GBIF_EMAIL=
MOVEBANK_USER=           # only needed for Phase 4 non-public / handshake
MOVEBANK_PASS=
ALLOW_NC=false           # keep CC BY-NC out unless explicitly true
