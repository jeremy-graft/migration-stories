# Don't shell out to `pnpm install` before every `pnpm run` script.
# The auto-check trips over esbuild/sharp build-script approval on Windows;
# builds are governed explicitly via package.json > pnpm.onlyBuiltDependencies.
verify-deps-before-run=false
